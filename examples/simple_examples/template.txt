from minio_async import Minio
import asyncio

client = Minio(
    endpoint="124.223.224.49:9000",
    access_key="hlf01",
    secret_key="1486922887",
    secure=False  # http for False, https for True
)

loop = asyncio.get_event_loop()

res=loop.run_until_complete(client.bucket_exists("bucket"))
if res:
    print('bucket exists')
else:
    print("bucket doesn't exist")
    
loop.close()